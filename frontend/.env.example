# Local development worker URL.
# In Vercel, replace this with your deployed worker base URL.
CLIPPER_WORKER_URL=http://localhost:4010

# Optional. Fill this only if the worker requires a shared token.
CLIPPER_WORKER_TOKEN=
